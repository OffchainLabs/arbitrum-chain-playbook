# Prometheus configuration for Nitro node metrics
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'nitro-exporter'
    static_configs:
      - targets: ['nitro-exporter:9091']
        labels:
          chain: 'target'
